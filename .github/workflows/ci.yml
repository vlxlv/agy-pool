name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  syntax-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check Python Syntax
        run: |
          python3 -m py_compile bin/agy-pool
          python3 -m py_compile scripts/live_test.py

      - name: Run tests
        run: |
          python3 -m unittest discover -s tests -v

      - name: Check Shell Scripts Syntax
        run: |
          bash -n bin/agy-raw
          bash -n install.sh
          bash -n uninstall.sh
          bash -n scripts/live-test.sh
